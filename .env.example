# ========================================
# SubsQ - Database Configuration
# ========================================
# このファイルはDocker Composeでのローカル開発環境用の設定です
# 本番環境ではAWS RDSなど別のデータベースを使用します

# ========================================
# PostgreSQL コンテナ設定
# ========================================
# Dockerコンテナ名
DB_CONTAINER_NAME=subsq-db

# データベースユーザー名
DB_USER=subsq_user

# データベースパスワード
# 警告: 本番環境では必ず強力なパスワードを使用してください
DB_PASSWORD=subsq_password_change_me

# データベース名
DB_NAME=subsq

# データベースポート（ホスト側）
# デフォルト: 5432
# 他のPostgreSQLと競合する場合は変更してください（例: 5433）
DB_PORT=5432

# ========================================
# Database URL (Drizzle ORM / Frontend用)
# ========================================
# フロントエンド（Next.js）から接続する際のデータベースURL
# frontend/.env.local にもこのURLをコピーしてください
# 注意: ${変数}の展開は環境によって動作が異なるため、実際の値に置き換えることを推奨
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@localhost:${DB_PORT}/${DB_NAME}

# 変数展開なしの例（実際の値に置き換えて使用）:
# DATABASE_URL=postgresql://subsq_user:subsq_password_change_me@localhost:5432/subsq